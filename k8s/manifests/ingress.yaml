apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /bar
            pathType: Exact
            backend:
              service:
                name: service1
                port:
                  number: 80
